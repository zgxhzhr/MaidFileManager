modLoader = "javafml" #mandatory
loaderVersion = "${forge_loader_version_range}" #mandatory This is typically bumped every Minecraft version by Forge. See https://files.minecraftforge.net/ for a list of versions.
license = "${license}" # Review your options at https://choosealicense.com/.
#issueTrackerURL="https://change.me.to.your.issue.tracker.example.invalid/" #optional
[[mods]] #mandatory
modId = "${mod_id}" #mandatory
version = "${version}" #mandatory
displayName = "${mod_name}" #mandatory
#updateJSONURL="https://change.me.example.invalid/updates.json" #optional (see https://mcforge.readthedocs.io/en/latest/gettingstarted/autoupdate/)
#displayURL="https://change.me.to.your.mods.homepage.example.invalid/" #optional (displayed in the mod UI)
# logoFile 已移除：工程内并不存在 maid_file_manager.png，声明它只会让 Forge 刷缺失图标警告
credits = "${credits}" #optional
authors = "${mod_author}" #optional
# 描述以 TOML Unicode 转义书写（等价于"车万女仆附属模组，支持跨版本导入导出女仆数据"）：
# gradle.properties 按 ISO-8859-1 读取，中文经属性占位符 expand 必然 Latin1 损坏，故描述不走占位符
# （注释中提到该占位符时写作 \${description}，避免被 expand 展开）；
# Groovy expand 会把「反斜杠 u 加四位十六进制」的 Unicode 转义按 Java 规则解码，
# 配合 buildSrc 的 filteringCharset=UTF-8，jar 内即为正确的 UTF-8 中文。
description = "\u8F66\u4E07\u5973\u4EC6\u9644\u5C5E\u6A21\u7EC4\uFF0C\u652F\u6301\u8DE8\u7248\u672C\u5BFC\u5165\u5BFC\u51FA\u5973\u4EC6\u6570\u636E" #mandatory
[[dependencies.${mod_id}]] #optional
modId = "forge" #mandatory
mandatory = true #mandatory
# 同步 loaderVersion 的兼容范围：Forge 1.20（46.x）与 1.20.1（47.x）均可加载
versionRange = "${forge_loader_version_range}" #mandatory
ordering = "NONE" # The order that this dependency should load in relation to your mod, required to be either 'BEFORE' or 'AFTER' if the dependency is not mandatory
side = "BOTH" # Side this dependency is applied on - 'BOTH', 'CLIENT' or 'SERVER'
[[dependencies.${mod_id}]]
modId = "minecraft"
mandatory = true
versionRange = "${minecraft_version_range}"
ordering = "NONE"
side = "BOTH"
[[dependencies.${mod_id}]]
modId = "touhou_little_maid"
mandatory = true
# 注意：TLM 的版本字符串常带前缀（如 1.5.3-forge+mc1.20.1），
# Forge Maven 版本比较器无法把带 "+mc1.20.1" 构建元数据的版本纳入 [1.5.0,) 范围，
# 故在此使用 * 表示"任意版本都接受"，实际功能仅依赖 TLM 存在 EntityMaid 等类。
versionRange = "*"
ordering = "AFTER"
side = "BOTH"
